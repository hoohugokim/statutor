<!-- statutor: plane=state | policy=state (doctor-checked) | writer=orchestrator | stable IDs, one line per task -->
# TASKS

- [x] T-0001 Port manual test battery to pytest (`tests/test_kernel.py`: hook, check, bash guard, init idempotence, staged a–e via tmp git repos)
- [x] T-0002 Name-collision sweep for `writ` (PyPI, npm, GitHub, crates); decide final CLI name; record as D-0009
- [x] T-0003 Verify Codex hook registration format on current release (hooks.json vs `[hooks]` table); pin exact snippet in adapters/codex/README.md
- [x] T-0004 Verify OpenCode subagent hook coverage (sst/opencode#5894) on current release; note result in adapters/opencode/README.md
- [x] T-0005 Add SessionEnd/Stop hook to the Claude adapter running `writ-doctor` and surfacing WARNs
- [x] T-0006 Integrate the existing Clawd 5-hour PreToolUse HANDOFF checkpoint hook so its output passes statutor validation (sections + stamp) — CLOSED AS OBSOLETE 2026-08-24 (D-0013): the hook never existed on disk (full sweep: settings, backups, app hooks, history); staleness is already stop_doctor's job and every HANDOFF write is validated; build nothing without a real failure
- [x] T-0007 Tag v0.2.0, push repo, test `/plugin marketplace add` + install flow end-to-end — DONE 2026-08-24: tag pushed & CI verified; plugin swap (marketplace re-point + statutor@hoo-plugins install) completed by human same day
- [x] T-0008 CI (GitHub Actions): pytest + self-dogfood `statutor staged` on this repo per PR
- [x] T-0009 writ_doctor: read budgets/sections from `.writ.yaml` instead of module constants
- [x] T-0010 Investigate loading statutor doctrine into Hermes Agent via its skills (agentskills.io format); enforcement there stays git-floor-only until a hook surface is confirmed — DONE 2026-08-24: pre_tool_call block surface confirmed, plugin adapter shipped; adapter then DROPPED same-day by the D-0012 scope cut (stays closed as history)
- [x] T-0011 Close the apply_patch gap: extend adapters/opencode/statutor.ts allowlist and decide kernel handling of Codex apply_patch `{command: <patch>}` payloads — DONE 2026-08-24: kernel guard_apply_patch parses envelopes (semantics in D-0011); allowlist/matcher widened; residual MCP + Update-section checks stay with the floor
- [x] T-0012 adapters/git/pre-commit vendored fallback points at `.statutor/statutor_core.py` which `statutor init` never creates — vendor it or drop the branch — DROPPED 2026-08-24 (vendoring forks the single kernel, D-0003): hook fails closed with install guidance; hermetic shim tests cover both branches
- [x] T-0013 Generalize statutor_doctor's remaining hardcoded names (DECISIONS.md status check, TASKS.md/plans consumed-plan heuristic) to policy-derived filenames — DONE 2026-08-24: state + append_only rules drive the two filenames (basename-only rule); plans/ stays conventional (init scaffolds it)
- [x] T-0014 Execute the rename once D-0010 records the new name: pyproject, plugin.json, `.writ.yaml` policy filename, hooks/commands/skills, READMEs, repo dir
- [x] T-0015 Claim PyPI `statutor`: build + twine-upload v0.2.0 (runbook plans/registry-claims.md); re-scope the API token to the project afterward
- [x] T-0016 Publish honest npm placeholder `statutor` 0.0.1 pointing at PyPI + this repo — SUPERSEDED 2026-08-24 by D-0014/T-0019/T-0022: the namespace carries the real OpenCode adapter instead
- [x] T-0017 Publish honest crates.io placeholder `statutor` 0.0.1 (lib crate, doc-comment reservation) — SUPERSEDED 2026-08-24 by D-0014/T-0020..T-0022: the namespace carries the real staged-floor crate instead
- [x] T-0018 After T-0015: flip repo public (`gh repo edit hoohugokim/statutor --visibility public`); optionally claim GitHub org + statutor.dev — DONE 2026-08-24: public debut confirmed via unauthenticated GitHub API; org/domains left unclaimed (optional)
- [x] T-0019 Publish real npm package `statutor` 0.1.0 (D-0014): adapters/opencode packaging, README install story (`"plugin": ["statutor"]`), pack + scratch-load verification; human runs publish — AGENT SIDE DONE 2026-08-24: tarball audited; E2E proven in scratch project (config-referenced node_modules load → bash-guard denial reaches the model as tool result); publish = T-0022
- [x] T-0020 Rust staged-mode floor crate `statutor` (bin `statutor-staged`, yaml-rust2): parity with post-fix run_staged semantics — incl. kernel `_git()` gaining `-c color.ui=false` and un-pinning the quirk test — DONE 2026-08-24: byte-compatible output incl. Python list-repr sections; fnmatch ported with python literal-'[' semantics; 4 rust unit tests
- [x] T-0021 Conformance harness (D-0014 license): scripts/gen_staged_fixtures.py (~30 fixtures from K-95..K-118 families), tests/test_conformance_rust.py differential runner, CI rust leg — DONE 2026-08-24 as scenarios-module design (tests/conformance_scenarios.py, no fixture files): 30 builders + hostile-gitconfig test; CI job `rust-conformance` gates on --locked build + cargo test + differential run
- [x] T-0022 Human publish flows: npm publish after T-0019 green; cargo publish only after the CI rust conformance leg is green — DONE 2026-08-24: npm statutor@0.1.0 + crates.io statutor 0.1.0 both LIVE (verified via registry APIs same day)
- [x] T-0023 Remove all tracked Cargo `target/` output; add crate/npm include + LICENSE payload assertions; prepare clean 0.1.1 artifacts without rewriting history — DONE 2026-08-31: removed 83 tracked build files; exact `.crate`/`.tgz` tests enforce 0.1.1 allowlists, LICENSE bytes, text-only members, and no private paths; CI runs the real pack audit; conformance returns its isolated fresh binary
- [x] T-0024 Record the trust-root policy: staged/HEAD policy selection, `.statutor.yaml` and CLAUDE→AGENTS bootstrap ownership, weakening rules, and the explicit human override path — DONE 2026-08-31: D-0015 defines HEAD/index snapshots, non-configurable trust roots, exact-bridge ownership, and an exact-tree approval receipt
- [x] T-0025 Close git-floor lifecycle bypasses in Python and Rust: governed delete/rename, append-only binary/attributes diffs, frozen arrivals, and git-command failures; add expected-result conformance cases — DONE 2026-08-31: NUL-safe raw HEAD/index line-subsequence checks replace rendered diffs; record deletion/rename-out and direct frozen arrival deny; Git/non-worktree failures deny; 42 Python↔Rust scenarios include independent expected outcomes; fresh cargo/full/package gates pass
- [x] T-0026 Fix cross-layer semantics: physical line counts, quoted YAML caps, explicit-CWD relative paths, sized Edit checks, staged-policy snapshotting, and optional-PyYAML install/docs — DONE 2026-08-31: dependency-free strict policy subset; HEAD+B/index+C dual judgment; `.statutor.yaml` and exact CLAUDE bridge receipts; Python/Rust 52-scenario parity; 326-test/full and package gates green
- [x] T-0027 Define and enforce `state` semantics for TASKS: valid unique stable IDs, permitted checkbox/detail edits, shell/apply_patch/staged behavior, and an eventual pruning story — DONE 2026-08-31: D-0016 preserves committed IDs while allowing checkbox/detail/order edits; new IDs advance; shell/editor/apply_patch/doctor/Python/Rust enforce; pruning deferred to identity-preserving archive decision
- [x] T-0028 Harden doctor/Stop: invalid dates never hide other drift, generic AGENTS-only repos stay silent, nested ledger roots resolve correctly, and stale `/ledger-init` text is removed — DONE 2026-08-31: marked-root discovery walks ancestors; AGENTS alone is silent; invalid/future dates aggregate with other findings; current `statutor init .` recovery text; dependency-free policy diagnostics
- [x] T-0029 Make public claims/install snippets exact: D-0017 confines the native twin to local staged-index use; docs use the real repository, v0.3.1 pin, and Bash|apply_patch matcher; regression tests reject placeholder, pre-receive, static, and optional-PyYAML CI drift
- [x] T-0030 Gate releases on tests plus built-artifact smoke checks and version parity — AGENT SIDE DONE 2026-08-31: Python/plugin 0.3.1 and npm/crate 0.1.1 candidates pass the exact-index gate (362/3, Rust 7, sdist/wheel/npm/crate payloads, isolated wheel CLI smoke); tag/push/publish remain explicit human actions
- [x] T-0031 Record the bounded v0.4 user-layer decision: D-0018 accepts human-owned canonical AGENTS/overlays/skills, whole-artifact Statutor projections, CAS/backups/receipts, current Claude 2.1.251/Codex 0.151.0/OpenCode v1.18.20 baselines, fake-home safety, and explicit non-goals
- [x] T-0032 Build the global host resolver, JSON config/receipt schema, deterministic tree hashing, atomic compare-and-swap writes, backups, and restore primitives — DONE 2026-08-31: zero-dependency `statutor_global`; explicit fake roots; strict config/state/artifact schemas; safe link-aware hashes; durable file/tree CAS; lock/journal; exact absent/file/tree/symlink backup restore; 30 focused tests
- [x] T-0033 Add opt-in global AGENTS lifecycle for Claude, Codex, and stable OpenCode — DONE 2026-08-31: deterministic whole-file projections; read-only plan/status JSON; every mutation pre-displays its plan; byte-preserving adopt; transactional apply/uninstall with initial backups, receipts, journals, rollback, CAS hand-edit refusal, precedence warnings, explicit fake roots, and CLI/package dispatch; 10 instruction + 30 substrate tests
- [x] T-0034 Add global Agent Skill lifecycle: Agent Skills validation, source-to-host projection, foreign-lock coexistence, duplicate classification, sync, safe adoption, and uninstall — DONE 2026-08-31: new-only exact import; portable + Claude tree projections; explicit identical adoption; foreign-lock exclusion; CAS sync/uninstall; receipts, journals, rollback; fake-root and installed-wheel coverage
- [x] T-0035 Extend status/doctor with stable JSON: effective precedence, shadowing, drift, modified targets, invalid skills, escaping/cyclic links, catalog budget, and legacy-root inventory — DONE 2026-08-31: fast status + deep doctor; stable inventory/exit semantics; Codex override/cap/disabled settings; eight skill roots; foreign/plugin/admin/system ownership; receipt/realpath drift; active duplicate conflicts; 32 KiB diagnostic catalog budget; 14 focused fake-root tests
- [x] T-0036 Test the user layer under fake homes and current real host releases, dogfood only by separate explicit approval, update adapter docs, then release v0.4 — AGENT SIDE DONE 2026-08-31: opt-in fake-profile E2E pins Claude 2.1.251/Codex 0.151.0/OpenCode 1.18.20; Codex prompt and OpenCode skill discovery pass, Claude's missing offline personal-skill inventory is explicit; modified-target refusal/uninstall recovery pass; adapter docs and 0.4.0 candidate pass the exact-index gate (434/3, Rust 7); real-home dogfood/tag/push/publish remain separate human actions
- [x] T-0037 Incorporate Telltales findings before v0.4.0 dogfood — DONE 2026-09-01: D-0019 makes automatic adapters silent outside the nearest `.statutor.yaml`, preserves explicit custom checks, resolves nested event paths against the ledger root, reports HEAD policy authority, and narrowly excludes complete quoted `git commit -F -` message bodies from strict bash scanning; Telltales regressions, installed-wheel opt-in smoke, Claude 2.1.252/Codex 0.151.0/OpenCode 1.18.20 E2E, exact-index gate (448/3, Rust 7), and a phase-gated real-home runbook pass; no real-home mutation/tag/push/publish performed
- [ ] T-0038 Specify v0.5 worker provenance: evidence vocabulary, random machine identity, HANDOFF revision lineage, repository/worktree identity, local schema, privacy boundary, host capabilities, and backward compatibility (D-0020..D-0022)
- [x] T-0039 Implement mode-0600 machine identity plus atomic local worker state and `statutor machine`/`statutor worker begin|show|active|record|complete|compare` commands with scoped stable JSON, CAS baselines, rotation, and lease expiry — DONE 2026-09-03: `core/statutor_worker.py` plus CLI dispatch, py-modules entry, and 15 hermetic tests; full gate 464 passed/2 skipped, doctor clean
- [ ] T-0040 Add optional worker/machine and `handoff_id`/`supersedes` metadata to HANDOFF scaffolds, doctor diagnostics, skill/command guidance, completion validation, and read-only Git-ref reconciliation guidance without invalidating existing ledgers
- [ ] T-0041 Integrate Claude, Codex, and OpenCode only to proven lifecycle surfaces; test crashes, linked worktrees, nested roots, cloned/rotated machines, sibling handoffs and reconciliation, unknown roles, capability reporting, and isolated current-release E2E
